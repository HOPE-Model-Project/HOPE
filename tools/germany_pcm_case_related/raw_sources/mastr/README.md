# MaStR Raw Drop

Place any Germany registry exports or manually curated MaStR extracts used for validation in this folder.

These files are optional for the first-pass fleet staging, but they should be recorded in `SOURCE_DATA_REGISTER.csv` if used.
